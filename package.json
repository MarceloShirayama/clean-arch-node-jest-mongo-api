{
  "name": "clean-arch-node-jest-mongo-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --noStackTrace",
    "test:unit": "jest --watch --silent -c ./jest-unit-config.js",
    "test:integration": "jest --watch --silent -c ./jest-integration-config.js",
    "test:staged": "jest -o --passWithNoTests --silent --verbose",
    "test:ci": "jest --coverage  --silent --verbose",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "eslint": "^7.32.0",
    "eslint-config-standard": "^16.0.3",
    "husky": "^7.0.0",
    "jest": "^27.0.6"
  },
  "dependencies": {
    "validator": "^13.6.0"
  }
}
