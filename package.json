{
  "name": "clean-arch-node-jest-mongo-api",
  "version": "1.0.0",
  "description": "",
  "main": "src/main/index.js",
  "scripts": {
    "start": "node .",
    "dev": "nodemon .",
    "test": "jest --noStackTrace",
    "test:unit": "jest --watch --silent --runInBand -c ./jest-unit-config.js",
    "test:integration": "jest --watch --silent --runInBand -c ./jest-integration-config.js",
    "test:staged": "jest -o --passWithNoTests --silent --verbose --runInBand",
    "test:ci": "jest --coverage  --silent --verbose --runInBand",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@shelf/jest-mongodb": "^2.0.3",
    "@types/jest": "^27.0.1",
    "@types/mongodb": "^4.0.7",
    "eslint": "^7.32.0",
    "eslint-config-standard": "^16.0.3",
    "husky": "^7.0.0",
    "jest": "^27.0.6",
    "nodemon": "^2.0.12",
    "supertest": "^6.1.6"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^4.1.0",
    "validator": "^13.6.0"
  }
}
